{
    "version": "2.0.0",
    "tasks": [
         {
            "label": "Build (Debug)",
            "type": "shell",
            "command": "cd build && cmake .. -DCMAKE_BUILD_TYPE=debug && make",
            "problemMatcher": []
         }
    ]
}