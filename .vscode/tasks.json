{
    "version": "2.0.0",
    "tasks": [
         {
            "label": "Build (Debug)",
            "type": "shell",
            "command": "cd build && cmake .. -DCMAKE_BUILD_TYPE=Debug -DCMAKE_POLICY_VERSION_MINIMUM=3.5 && make -j && cp compile_commands.json ..",
            "problemMatcher": []
         }
    ]
}